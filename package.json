{
  "name": "@tsmx/human-readable",
  "version": "1.0.0",
  "description": "Easily create human-readable strings for byte sizes and other units, e.g. 17238 --> 17.24 kB. Supports decimal (MB,GB,..) and IEC (MiB, GiB,..) units.",
  "main": "human-readable.js",
  "scripts": {
    "test": "jest",
    "test-coverage": "jest --coverage",
    "coveralls": "jest --coverage && coveralls < coverage/lcov.info"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/tsmx/human-readable.git"
  },
  "author": "tsmx <dev@tsmx.net>",
  "license": "MIT",
  "devDependencies": {
    "coveralls": "^3.1.0",
    "jest": "^26.4.2"
  }
}
