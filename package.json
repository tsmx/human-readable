{
  "name": "@tsmx/human-readable",
  "version": "1.0.0",
  "description": "Converting byte sizes to humand-readable strings. Supports decimal and IEC unit conversion.",
  "main": "human-readable.js",
  "scripts": {
    "test": "jest",
    "test-coverage": "jest --coverage",
    "coveralls": "jest --coverage && coveralls < coverage/lcov.info"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/tsmx/human-readable.git"
  },
  "author": "tsmx <dev@tsmx.net>",
  "license": "MIT",
  "devDependencies": {
    "coveralls": "^3.1.0",
    "jest": "^26.4.2"
  }
}
